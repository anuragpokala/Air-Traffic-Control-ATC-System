/**
 * Node for the Skip List.
 * Stores an AirObject and its name key.
 * 
 * @author Anurag Pokala (anuragp34) 
 * @author Parth Mehta (pmehta24)
 * @version 12/3/2025
 */
public class SkipNode {
    
    private String key;
    private AirObject element;
    private SkipNode[] nextNodes;

    /**
     * Create a node with given key, value, and level.
     * 
     * @param k Key
     * @param elem Value
     * @param level Number of levels this node participates in
     */
    public SkipNode(String k, AirObject elem, int level) {
        key = k;
        element = elem;
        nextNodes = new SkipNode[level];
    }

    /**
     * Get the key.
     * @return The key
     */
    public String getKey() {
        return key;
    }

    /**
     * Get the value.
     * @return The value
     */
    public AirObject getValue() {
        return element;
    }

    /**
     * Get forward pointer at given level.
     * @param level The level
     * @return The node at that level's forward pointer
     */
    public SkipNode getNext(int level) {
        return nextNodes[level];
    }

    /**
     * Set forward pointer at given level.
     * @param level The level
     * @param node The node to point to
     */
    public void setNext(int level, SkipNode node) {
        nextNodes[level] = node;
    }

    /**
     * Get the number of levels this node has.
     * @return Number of levels
     */
    public int getLevels() {
        return nextNodes.length;
    }
}
